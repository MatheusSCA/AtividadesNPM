const express = require('express');
const app = express();
const port = 8000;

MaiorQ(arr);{
    arr.sort()
    let Maior = arr[arr.length - 1], secbigger

    let i = arr.length - 2
    while(arr[i] == Maior) i--;

    secbigger = arr[i]

    return { Maior,Maior2 }
  }

let arr = [1, 4, 5, 6 ,6, 6,  2, 2, 3, 4,]
console.log(MaiorQ(arr))